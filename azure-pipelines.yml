trigger:
- main  # Change to your branch

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    echo "🔍 Checking environment variables..."
    echo "🔍 AZURE_DEVOPS_PAT length: ${#AZURE_DEVOPS_PAT}"  # Should print a number
    echo "🔍 GITHUB_PAT length: ${#GITHUB_PAT}"

    # ✅ Export variables explicitly to ensure Python picks them up
    export AZURE_DEVOPS_PAT="${AZURE_DEVOPS_PAT}"
    export GITHUB_PAT="${GITHUB_PAT}"

    # ✅ Print variables inside Python
    python -c "import os; print(f'AZURE_DEVOPS_PAT: {os.getenv("AZURE_DEVOPS_PAT")}')"
    
    python -m pip install --upgrade pip
    python sync_to_github.py
  env:
    AZURE_DEVOPS_PAT: $(AZURE_DEVOPS_PAT)  # ✅ Ensure secrets are passed
    GITHUB_PAT: $(GITHUB_PAT)
  displayName: "Run Python Sync Script"